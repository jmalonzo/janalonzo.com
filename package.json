{
  "name": "janalonzo.com",
  "version": "3.4.0",
  "private": true,
  "dependencies": {
    "vuepress-theme-yuu": "^2.2.1"
  },
  "devDependencies": {
    "@vuepress/plugin-pwa": "^1.2.0",
    "vuepress": "^1.2.0"
  },
  "scripts": {
    "clean": "rm -rf public static build tmp dist",
    "html-minifier": "html-minifier --remove-comments --collapse-whitespace --input-dir public",
    "copy-assets": "cp assets/* public/",
    "build": "npm run clean && vuepress build content",
    "start": "vuepress dev content"
  }
}
