{
  "name": "janalonzo.com",
  "version": "3.0.0",
  "private": true,
  "devDependencies": {
    "html-minifier": "^3.5.2"
  },
  "dependencies": {},
  "scripts": {
    "clean": "rm -rf public static build tmp",
    "html-minifier": "html-minifier --remove-comments --collapse-whitespace --input-dir public",
    "copy-assets": "cp assets/* public/",
    "build": "npm run clean && hugo --uglyURLs='true' --theme hugo-theme-blueberry-detox && npm run copy-assets",
    "start": "hugo server --config config.dev.toml --buildDrafts --theme hugo-theme-blueberry-detox"
  }
}
