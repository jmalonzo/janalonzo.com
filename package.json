{
  "name": "janalonzo.com",
  "version": "3.4.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && npx @11ty/eleventy --output=dist && cp _assets/* dist/",
    "start": "npx @11ty/eleventy --serve"
  }
}
